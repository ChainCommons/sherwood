# Claude Code — Sherwood

Follow `AGENTS.md` and `plans/agents/PREAMBLE.md`.

## Commits

Do not add `Co-authored-by`, `Claude-Session`, or any mention of Claude, Anthropic, or other AI tools to a commit message. `includeCoAuthoredBy` must stay false. If the commit-msg hook rejects you, rewrite the message; never `--no-verify`.
